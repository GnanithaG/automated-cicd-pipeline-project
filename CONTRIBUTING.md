# Contributing

Create a feature branch, add tests for behavioral changes, and open a pull request. The pull request must pass Java tests, Python tests, static analysis, coverage gates, and container builds before merge.
